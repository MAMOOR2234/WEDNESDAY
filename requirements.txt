# Wednesday AI Assistant - Requirements
# Install with: pip install -r requirements.txt

# AI/LLM APIs
anthropic>=0.100.0          # Claude API (main brain)
openai>=1.0.0               # OpenAI API (fallback)

# GUI (Optional but recommended)
PyQt6>=6.0.0                # GUI Framework (JARVIS-style interface)

# Speech Recognition
# Choose one or both:
faster-whisper>=0.10.0  # Faster local transcription (OpenAI Whisper)
SpeechRecognition>=3.10.0  # Google Speech Recognition API
pyaudio>=0.2.13  # Microphone input (optional)
sounddevice>=0.4.6  # Microphone input (easier install, used for clap detection)

# Text-to-Speech
# Choose one or both:
pyttsx3>=2.90  # Built-in offline TTS
edge-tts>=6.1.0  # Microsoft Edge TTS (online)

# Windows Control
pyautogui>=0.9.53  # Keyboard/mouse automation
pyperclip>=1.8.2  # Clipboard operations

# Utilities
python-dotenv>=1.0.0  # Environment variables
requests>=2.28.0  # HTTP requests
psutil>=5.9.0  # System monitoring (CPU, memory, disk)

# Spotify integration
spotipy>=2.23.0             # Spotify Web API client

# Optional: For better performance and features
numpy>=1.24.0  # Scientific computing (used by faster-whisper)
pillow>=9.0.0  # Image processing (screenshots)
pytesseract>=0.3.10  # OCR (text detection in images)
